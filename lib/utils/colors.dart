// lib/constants/app_colors.dart

import 'package:flutter/material.dart';

/// Arcadian Café Color Palette for Light Theme

class AppColors {
  // Primary Brand Color - Dark Brown
  // Use for AppBar, primary buttons, headers, active bottom nav icon
  static const Color darkBrown = Color(0xFF4B2E2E);

  // Secondary / Accent - Latte / Medium Brown
  // Use for highlights, badges, category icons, floating action button background
  static const Color latte = Color(0xFFA67C52);

  // Background - Cream / Off-White
  // Use for screen background, cards, general containers
  static const Color creamBackground = Color(0xFFFFF8F0);

  // Text Primary - Dark Charcoal
  // Use for body text, menu item names, order details
  static const Color darkCharcoal = Color(0xFF333333);

  // Text Secondary - Medium Brown
  // Use for item descriptions, secondary text, placeholders
  static const Color mediumBrown = Color(0xFF6B4C3B);

  // Buttons / Action Highlights - Cream / Beige
  // Use for CTA buttons (e.g., "Proceed to Checkout"), FABs, selected tabs
  static const Color creamBeige = Color(0xFFEAD9C0);

  // Divider / Card Shadow - Soft Brown / Gray
  // Use for card borders, section dividers, subtle shadows
  static const Color softBrownGray = Color(0xFFD9C5B0);
}
